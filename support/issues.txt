* all QPKG statuses are shown as 'inactive' when viewing status table in verbose mode.
	Even when all QPKGs show as 'active' in non-verbose mode.

* QPKG version sort is not correctly sorting version numbers followed by an alpha as later than non-alpha.

* if a forked action hangs, it's not being killed by its caller.
	need to confirm this.
